#!/usr/bin/env python3
"""Annoctated function that takes
a float argument and returns the
floor of the float."""


def floor(n: float) -> int:
    """Takes float argument and returns
    int: int(n)"""
    return int(n)
