#!/usr/bin/env python3
"""Annotated function that takes
strings str1 and str2 as arguments and returns
a concatenated string."""


def concat(str1: str, str2: str) -> str:
    """Concatenate two strings
    retuns str: str1 + str2."""
    return str1 + str2
